// /vietst-mvp/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // appDir: true,   // 👈 App Router 활성화! (필수)
  },
};

module.exports = nextConfig;
